/**Basic constants */

const reachBottomSlack = 50;
const charactersUrl = "https://rickandmortyapi.com/api/character"

/**HTML ELEMENTS */
const splashScreen = document.getElementById('splash');
const searchToolbar = document.getElementById('search');
const holder = document.getElementById("hero_holder");


const aliveCheckBox = document.getElementById('alive-check');
const deadCheckBox = document.getElementById('dead-check');
const unknownCheckBox = document.getElementById('unknown-check');